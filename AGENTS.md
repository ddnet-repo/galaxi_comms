This is the muster CLI — a tool for assembling AI agent teams with character-driven personalities for OpenCode.

Key directories:
- `bin/muster` — CLI entry point
- `lib/` — all command implementations
- `.muster/` — pre-built team recipes (JSON) and recipe docs
