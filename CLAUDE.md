This is the muster CLI — a tool for assembling AI agent teams with character-driven personalities.

Key directories:
- `bin/muster` — CLI entry point
- `lib/` — all command implementations
- `templates/` — protocol template used by `muster init`
- `comms/` — example comms structure and docs
